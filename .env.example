# NeonDB Configuration
DATABASE_URL=postgresql://username:password@ep-xxxx.us-east-2.aws.neon.tech/aquanexus?sslmode=require

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# Appwrite Storage (kept for file uploads)
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your-project-id
APPWRITE_API_KEY=your-appwrite-api-key

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# AI Configuration (optional - can be configured via Settings page)
# These are fallback values; users configure their own endpoints in Settings
# DEFAULT_AI_MODEL1_NAME=gpt-o3-mini
# DEFAULT_AI_MODEL2_NAME=deepseek-r1
